import { StyleSheet } from 'react-native';

import { Spacing } from 'src/utils/styles';

const styles = StyleSheet.create({
  text: {
    fontSize: Spacing.size_40,
    lineHeight: Spacing.size_45,
    padding: Spacing.size_30,
    textAlign: 'center',
  },
});

export default styles;
