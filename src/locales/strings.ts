export enum Global {
  cancel = 'cancel',
  done = 'done',
}
export enum Settings {
  language = 'language',
  reviewRedditPhrases = 'reviewRedditPhrases',
  sendSuggestion = 'sendSuggestion',
}

export enum Reddit {
  discard = 'discard',
  emptyList = 'emptyList',
  score = 'score',
  save = 'save',
}
